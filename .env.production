# Production Environment - Configuration Only (No Secrets!)
# Used when deployed to Render
# 
# ⚠️  IMPORTANT: This file is tracked in git
# Only contains NON-SECRET configuration
# Set secrets in Render dashboard (see .env.production.secrets.example)
#
# ===========================================

# ===========================================
# Server Configuration (PUBLIC)
# ===========================================
PORT=3001
NODE_ENV=production

# ===========================================
# URLs (PUBLIC)
# ===========================================

# Production frontend URL
FRONTEND_URL=https://unsugar.io

# Production API URL (self-reference)
API_BASE_URL=https://unsugar-io-api.onrender.com

# CORS origins (comma-separated, include all frontend domains)
CORS_ORIGIN=https://unsugar.io,https://unsugar-io.netlify.app

# ===========================================
# Feature Flags (PUBLIC - Configuration)
# ===========================================

# SSO Features (disabled by default)
ENABLE_GOOGLE_SSO=false
ENABLE_GITHUB_SSO=false
ENABLE_APPLE_SSO=false

# ===========================================
# SECRETS (Set in Render Dashboard):
# ===========================================
# These must be set as Environment Variables in Render:
# 
# Required:
#   - MONGODB_URI (MongoDB Atlas connection string)
#   - JWT_ACCESS_SECRET (64+ char random string)
#   - JWT_REFRESH_SECRET (64+ char random string)
# 
# Optional (for SSO):
#   - GOOGLE_CLIENT_ID
#   - GOOGLE_CLIENT_SECRET
#   - GITHUB_CLIENT_ID
#   - GITHUB_CLIENT_SECRET
# 
# See: .env.production.secrets.example for template
# ===========================================
